
server:
    port: 10002
    servlet:
        context-path: /api
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/TestDB
    username: postgres
    password: postgres